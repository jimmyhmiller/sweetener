// An operator is a macro you can write between things, with the precedence
// and associativity you give it.
export operator (|>):expr {
  fixity infix;
  associativity left;
  precedence 40;

  rule { $value:expr |> $callee:ident } => {
    $callee($value)
  }
}
