# Generated Artifacts

Commands write transient test, benchmark, and review output under this directory.
Git tracks this README and directory placeholders. Git ignores generated files.

```text
test-results/
benchmarks/
review/
coverage/
```

Status commands mark a result stale when its recorded commit differs from the
current commit.
