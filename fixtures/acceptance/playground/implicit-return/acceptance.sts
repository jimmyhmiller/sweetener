import { function } from "./declarative.sts" for syntax shadows core;

export const calculate = function(value: number) {
  const doubled = value * 2;
  doubled + 1
};

export const result = calculate(3);
