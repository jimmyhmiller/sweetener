# Legacy Sweet.js Corpus

Codex imported these files from
`/Users/jimmyhmiller/Documents/Code/PlayGround/sweetjs` on 2026-08-02 after the
repository owner requested that the playground define mandatory syntax
capabilities.

Treat this directory as source evidence. Do not rewrite these files into the new
macro notation. TypeScript-targeted cases live under
`fixtures/acceptance/playground`.

`manifest.json` records each imported file's SHA-256 digest. The import retained
source content and line endings; three source files lacked a final newline, and
the patch workflow added one to those copies.
