# CMP-006 Evidence

Status: Done

Prerequisites: `CMP-005`

Specification: `docs/tasks/phase-05-composition.md`, CMP-006

Current work:

- enumerate every executable acceptance macro definition;
- reject imports from compiler internals or host modules;
- reject procedural callbacks, direct syntax-object construction, dynamic host
  evaluation, and filesystem/network/process access;
- run the audit in the ordinary repository gate with focused positive and
  negative tests.

Evidence:

- `scripts/declarative-boundary.mjs` exposes a reusable source audit and a
  repository-wide CLI;
- nine focused tests cover allowed declarative output plus compiler imports,
  host imports, compiler helpers, host execution, and raw syntax records;
- every checked-in acceptance `declarative.sts` definition passes;
- the audit runs in `pnpm check` and the package-boundary gate;
- 574 tests pass across 82 files with all repository gates green.

Next action: task complete; begin `TSH-001` macro-module resolution.
