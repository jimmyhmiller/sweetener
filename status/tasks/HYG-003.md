# HYG-003 Evidence

Status: Done

Prerequisite: `HYG-002`

Specification: `docs/tasks/phase-03-hygiene-templates.md`, HYG-003

Delivered:

- model macro-introduction scope flipping around transformer input and output;
- model use-site scopes on captured invocation syntax;
- prove definition-site identifiers remain separate from call-site bindings;
- test local macro declarations and uses within one lexical region;
- test generated declarations and later generated uses without capturing input;
- record the chosen rule in ADR-0006 and update the hygiene specification.

Evidence:

- each invocation allocates distinct introduction and use-site scopes;
- transformer input receives both scopes and output flips the introduction
  scope;
- copied captures retain call-site scopes plus use-site scope and lose the
  temporary introduction scope;
- template syntax retains definition scopes and gains introduction scope;
- captured local macro declarations and captured uses share their invocation
  region;
- generated declarations and generated uses resolve through shared
  introduction and lexical scopes, while captured caller syntax does not;
- nested invocations preserve outer use-site scopes and allocate distinct pairs;
- ADR-0006 records the accepted transform and measured alternatives;
- the normative hygiene specification replaces `OPEN-HYG-001` with the accepted
  seven-step rule;
- 214 tests pass across 44 files;
- formatting, lint, typecheck, boundaries, and acceptance checks pass.

Next action: implement binding resolution in `HYG-004`.
