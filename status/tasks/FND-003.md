# FND-003 Evidence

Status: Done

Prerequisite: `FND-002`

Specification: `docs/specifications/01-compiler-architecture.md`, section 8

Delivered:

- validated `SWR1xxx` through `SWR7xxx` diagnostic codes;
- one owner and a compatible compiler stage for each code range;
- required documentation entries and duplicate-code rejection;
- primary, related, and expansion-stack origins;
- structured message arguments and deterministic text rendering.

Evidence:

- focused tests cover invalid codes, owner and stage mismatches, missing
  documentation, duplicate registration, malformed spans, unknown codes, sorted
  registry output, structured fields, and rendered output;
- 33 unit tests pass across 19 files;
- repository typecheck and lint pass.

Next action: use structured diagnostic expectations in the `FND-004` fixture
harness.
