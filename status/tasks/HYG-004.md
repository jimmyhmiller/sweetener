# HYG-004 Evidence

Status: Done

Prerequisite: `HYG-003`

Specification: `docs/tasks/phase-03-hygiene-templates.md`, HYG-004

Delivered:

- filter environment candidates by identifier scope-set inclusion;
- remove strict-subset candidates and retain maximal sets;
- return immutable resolved, unbound, or ambiguity records;
- emit deterministic `SWR3001` diagnostics with declaration origins;
- sort ambiguity candidates by structural scope sets and declaration origins;
- prove insertion-order independence through 100 fixed-seed shuffles;
- cover phase, syntax-space, lexical-position, equal-set, and incomparable-set cases.

Evidence:

- 221 tests pass across 45 files;
- formatting, lint, typecheck, and focused resolver tests pass.

Next action: consume resolved identities from template hygiene operations in
`TPL-003`.
