# ENF-002 Evidence

Status: Done

Prerequisites: `ENF-001`, `RDR-004`

Specification: `docs/tasks/phase-04-expansion.md`, ENF-002

Delivered:

- identifier, keyword-literal, scalar-literal, array, object, template, and
  parenthesized primary consumption;
- iterative public/private member, index, call, optional-chain, non-null, and
  tagged-template postfix consumption;
- exact source-node retention with composed origins and primary precedence;
- caller stop-boundary handling without boundary consumption;
- structured failures for incomplete members, optionals, and indexes;
- pinned-TypeScript differential parsing for representative complete extents.

Evidence:

- 24 valid primary and postfix forms reconstruct byte-for-byte;
- original cursors remain untouched and result cursors reach exact extents;
- optional calls, properties, and indexes compose through longer chains;
- optional-chain tagged templates and empty index access are rejected;
- multi-node expressions receive composed source origins;
- six differential cases parse under TypeScript 6.0.2;
- 320 tests pass across 55 files;
- formatting, lint, typecheck, boundaries, and acceptance checks pass.

Next action: implement the Pratt expression parser in `ENF-003`.
