# TSH-004 Evidence

Status: Done

Prerequisites: `TSH-003`, `SYN-002`

Specification: `docs/tasks/phase-06-typescript-host.md`, TSH-004

Current work:

- intersect TypeScript generated spans with token-level origin segments;
- distinguish copied, introduced, synthesized, and composed origins;
- select stable primary source spans from the origin graph;
- attach macro-definition locations and invocation stacks;
- retain the original TypeScript code and related information.

Evidence:

- generated diagnostic spans intersect token-level origin-map regions;
- invocation-oriented primaries cover copied, introduced, and synthesized
  syntax while composed regions retain additional source locations;
- original TypeScript codes, categories, messages, and generated locations are
  preserved;
- introduced syntax relates the macro template definition and callers can
  attach immutable expansion stacks;
- diagnostics with no generated location remain intact and unmapped.

Validation: 590 tests pass across 87 files with all substantive gates green.

Next action: task complete; begin `TSH-005` source-map composition.
