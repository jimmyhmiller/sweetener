# TSH-002 Evidence

Status: Done

Prerequisites: `HYG-006`, `EXP-004`

Specification: `docs/tasks/phase-06-typescript-host.md`, TSH-002

Current work:

- print expanded syntax with deterministic hygienic names;
- group protected expressions when surrounding precedence requires it;
- record generated offsets back to syntax origins;
- serialize immutable expansion traces beside generated text;
- prove stable output across repeated builds.

Completed checkpoint:

- generated files apply the deterministic hygienic name-assignment plan;
- protected expression roots can be grouped at host-requested boundaries;
- ordered generated ranges retain their syntax origin identities;
- trace serialization recursively canonicalizes object keys;
- repeated output is covered by focused host-package tests.

Final evidence:

- origin segments are emitted per printed token and delimiter, including
  hygienic replacements and trivia;
- protected expressions compare their recorded precedence with the surrounding
  boundary requirement and group only when necessary;
- output and recursively canonicalized traces are byte-stable;
- 585 tests pass across 86 files with every repository gate green.

Next action: task complete; begin `TSH-003` CompilerHost integration.
