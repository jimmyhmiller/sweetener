# TSH-001 Evidence

Status: Done

Prerequisites: `CMP-006`

Specification: `docs/tasks/phase-06-typescript-host.md`, TSH-001

Evidence:

- define the versioned macro-module manifest and export model;
- resolve relative modules, package exports, and configured path aliases;
- build separate immutable compile-time and runtime dependency graphs;
- reject unsupported versions, missing exports, ambiguous paths, and
  compile-time cycles with stable diagnostics;
- test deterministic resolution and graph behavior without host execution.

- manifests use a closed format with named/category/phase exports, compiler
  compatibility, and explicitly typed macro/runtime dependencies;
- resolution covers relative paths, wildcard aliases, and package export maps;
- imported names are checked against manifest exports before compilation;
- macro and runtime edges are immutable and separate; only macro cycles fail;
- shared diagnostics cover invalid manifests, resolution failures, version
  mismatch, missing exports, ambiguous aliases, and compile-time cycles.

Next action: task complete; begin `TSH-002` expanded TypeScript printing.
