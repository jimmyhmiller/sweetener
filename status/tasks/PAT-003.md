# PAT-003 Evidence

Status: Done

Prerequisite: `PAT-002`

Specification: `docs/tasks/phase-02-patterns.md`, PAT-003

Delivered:

- define immutable, serializable matcher instructions and program metadata;
- assign deterministic program counters and capture slots;
- lower groups, choices, repetitions, optionals, literals, class calls, and
  lookahead without recursive compiler traversal;
- preserve source origins for later failure ranking and explanation traces.

Evidence:

- immutable instruction and program records with branded program counters and
  capture slots;
- continuation-based lowering for literals, captures, class calls, lookahead,
  groups, sequences, choices, optionals, and separated repetition;
- explicit compilation task stack handles 5,000 nested pattern nodes;
- groups terminate through local group-accept instructions;
- repetition retains stable IDs, bounds, body, separator, loop, and exit PCs;
- capture slots sort by deterministic capture ID;
- invalid shape inference cannot reach compilation;
- every instruction reference is checked in bounds;
- every declarative acceptance rule compiles twice to byte-identical JSON;
- 173 tests pass across 38 files;
- formatting, lint, typecheck, boundaries, and acceptance checks pass.

Next action: execute matcher programs in `PAT-004`.
