# PAT-007 Evidence

Status: Done

Prerequisite: `PAT-006`

Specification: `docs/tasks/phase-02-patterns.md`, PAT-007

Delivered:

- define immutable refinement predicates for token kind, spelling and case,
  boundaries, selected alternatives, repetition length, and delimiters;
- parse refinement clauses into fixed IR without host-language callbacks;
- evaluate refinements against class-rule captures and match metadata;
- report malformed targets, predicates, and incompatible capture shapes.

Evidence:

- immutable predicates cover token kinds, spelling equality and sets, Unicode
  case, boundaries, selected alternatives, repetition lengths, and delimiters;
- constructors normalize sets and reject empty sets, invalid lengths, mutable
  boundary literals, and negative alternative indexes;
- evaluators traverse nested capture sequences without recursive calls and
  apply leaf predicates to each captured element;
- class compilation resolves target names to rule-local capture IDs and reports
  missing targets as SWR2010;
- class execution rejects a matched rule when any refinement fails;
- the macro-language adapter lowers `spelling starts-with-lowercase` and its
  uppercase counterpart to fixed IR;
- the ADT constructor-pattern acceptance refinement compiles with the rest of
  the declarative class corpus;
- tests cover all predicate families, repeated captures, Unicode case,
  malformed predicates, class filtering, and concrete-syntax lowering;
- 195 tests pass across 41 files;
- formatting, lint, typecheck, boundaries, and acceptance checks pass.

Next action: port structural playground examples in `PAT-008`.
