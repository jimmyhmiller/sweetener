# TLS-001 Evidence

Status: Done

Prerequisites: `TSH-005`

Specification: `docs/tasks/phase-07-tooling-release.md`, TLS-001

Current work:

- query all origin regions containing a generated position;
- query all generated regions derived from an original source position;
- expose copied, introduced, synthesized, composed, and grouping
  classification;
- locate the innermost macro invocation and expansion stack for a region;
- define stable boundary behavior for token starts/ends and generated gaps.

Evidence:

- generated queries return every containing region with primary/all original
  sources, classification, and expansion stack;
- original queries return every generated occurrence, including repeated
  captures;
- copied, introduced, composed, source, and grouping regions remain distinct;
- half-open token ranges, exact ends, generated gaps, and file bounds have
  explicit tested behavior;
- invalid or unknown origin maps fail during immutable index construction.

Validation: 613 tests pass across 95 files with all substantive gates green.

Next action: task complete; begin `TLS-002` expand and explain.
