# TPL-004 Evidence

Status: Done

Prerequisites: `TPL-003`, `SYN-002`

Specification: `docs/tasks/phase-03-hygiene-templates.md`, TPL-004

Delivered:

- apply invocation introduction scope to literal template syntax;
- preserve capture and explicit call-site or definition-site scopes;
- allocate copied and template origins;
- materialize fresh, text, and index values as syntax;
- construct balanced immutable groups and sequences;
- preserve delimiter prototypes for templates and JSX;
- allocate deterministic fresh binding identities;
- enforce output-token, expansion-step, nesting, deadline, and cancellation limits.

Evidence:

- literal identifiers receive definition scopes plus introduction scope;
- ordinary captures receive caller scopes plus use-site scope and no
  introduction scope;
- `callsite`, `definition`, and deliberate `capture` produce distinct asserted
  scope sets and composed origins;
- fresh, text, and index values materialize with correct token kinds;
- groups remain balanced without delimiter prototypes;
- output exhaustion and cancellation return no partial result;
- 246 tests pass across 48 files;
- formatting, lint, typecheck, boundaries, and acceptance checks pass.

Next action: apply declarative binding contracts in `HYG-005`.
