# REL-001 Evidence

Status: Done

Prerequisites: `TSH-008`

Specification: `docs/tasks/phase-07-tooling-release.md`, REL-001

Delivered:

- executable support diagnostics for Node 24.x and TypeScript API 6.0.x;
- stable `SWR7001` and `SWR7002` failures outside supported lines;
- a CI cross-product for Node 24.0.0/24.18.1 and compatibility packages 6.0.0/6.0.2;
- a recorded matrix and adapter-difference report;
- isolated oldest-endpoint installation without changing the development lockfile.

Validation:

- Node 24.0.0 with `@typescript/typescript6@6.0.0` passes all 633 tests, type checking, package boundaries, declarative boundaries, and acceptance contracts;
- Node 24.18.1 with the pinned package passes the toolchain probe and ordinary repository gates;
- both npm endpoint packages expose API version 6.0.3; no scanner or host differences were observed.

Next action: keep the full matrix required on pull requests and main pushes.
