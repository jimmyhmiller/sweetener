# CMP-003 Evidence

Status: Done

Prerequisites: `ENF-003`, `EXP-001`

Specification: `docs/tasks/phase-05-composition.md`, CMP-003

Delivered:

- parsed fixity, precedence, and associativity now lower into checked operator
  entries alongside their compiled macro identity;
- generated operators carry the same table entry through definition re-entry;
- a reusable lexical resolver connects persistent operator tables to Pratt,
  including multi-token punctuation, precedence, lexical visibility, and core
  interception authorization;
- real declarative matcher/template invocation through Pratt for prefix and infix
  forms, including authorized core interception;
- immutable grouping traces with binding, fixity, binding power, operand
  origins, and result origin;
- structured import/local conflicts with atomic rollback and related origins;
- nonassociative-chain diagnostics, lexical scope isolation, TypeScript type
  inference, emitted-source comparison, and runtime execution evidence;
- explicit fixity declarations across every accepted operator fixture.

Validation: 496 tests pass across 70 files. Formatting, lint, TypeScript build,
package boundaries, acceptance contracts, fixture compilation, grouping,
malformed input, conflict rollback, static semantics, and runtime checks pass.

Next action: preserve these contracts while validating mixfix composition and
porting the combined playground suite.
