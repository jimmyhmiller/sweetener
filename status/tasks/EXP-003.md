# EXP-003 Evidence

Status: Done

Prerequisites: `PAT-005`, `TPL-004`, `EXP-002`, `ENF-001`

Specification: `docs/tasks/phase-04-expansion.md`, EXP-003

Delivered:

- compiled macro binding, rule, invocation, boundary, and trace contracts;
- source-ordered ordinary rule attempts followed by stable fallback attempts;
- ranked failure merging and unchanged caller cursors after failed invocation;
- boundary admission before invocation or contract scope allocation;
- shared-budget matching, binding contracts, declarative template evaluation,
  hygienic instantiation, and recursive category expansion;
- immutable traces for attempts, captures, scopes, operations, and origins;
- package-owned `SWR4001` diagnostics for unmatched macro invocations.

Evidence:

- fallback, ordinary source order, and selected-rule traces pass;
- rejected boundaries leave no invocation-scope residue;
- binding contracts, `#fresh`, and `#capture` pass through the full pipeline;
- matcher usage is charged once to the caller's shared resource tracker;
- recursive results are category checked and receive following scopes;
- no-match results preserve the original cursor and emit stable diagnostics;
- 363 tests pass across 57 files;
- formatting, lint, typecheck, package boundaries, and acceptance checks pass.

Next action: add termination and resource enforcement in `EXP-004`.
