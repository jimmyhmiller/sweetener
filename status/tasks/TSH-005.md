# TSH-005 Evidence

Status: Done

Prerequisites: `TSH-003`

Specification: `docs/tasks/phase-06-typescript-host.md`, TSH-005

Current work:

- decode TypeScript JavaScript and declaration source maps;
- join generated `.ts` positions with `.sts` origin-map regions;
- encode deterministic composed maps with original source identities;
- retain generated gaps without inventing misleading source positions;
- test token boundaries, trivia, repeated captures, JavaScript maps, and
  declaration maps.

Evidence:

- source-map VLQ segments are decoded, joined through generated TypeScript
  line/column offsets and origin regions, then deterministically re-encoded;
- source tables and optional original contents derive from stable source IDs;
- unmapped trivia/generated gaps remain unmapped instead of borrowing a
  misleading neighbor;
- repeated copied captures map back to the same original span;
- malformed mappings fail deterministically before artifact publication.

Validation: 594 tests pass across 88 files with all substantive gates green.

Next action: task complete; begin `TSH-006` caches and invalidation.
