# TPL-001 Evidence

Status: Done

Prerequisites: `PAT-002`, `MCL-001`

Specification: `docs/tasks/phase-03-hygiene-templates.md`, TPL-001

Delivered:

- define immutable template AST records;
- parse literal syntax, capture references, field paths, groups, and repetition;
- validate capture availability against pattern shapes;
- reject references at incompatible repetition depths;
- resolve public syntax-class fields to stable capture IDs;
- preserve outer repetition dimensions across field projection;
- reject missing and incompatible repetition drivers;
- compile parsed macro-rule templates after syntax-class compilation;
- add focused parser, validation, integration, and malformed-definition tests.

Evidence:

- 228 tests pass across 46 files;
- formatting, lint, typecheck, boundaries, and acceptance checks pass;
- diagnostics `SWR2011` through `SWR2016` cover malformed template definitions.

Next action: execute the validated template IR in `TPL-002`.
