# HYG-005 Evidence

Status: Done

Prerequisite: `TPL-004`

Specification: `docs/tasks/phase-03-hygiene-templates.md`, HYG-005

Delivered:

- parse binder and region paths into stable capture IDs;
- expand composite syntax-class binders through declared binding fields;
- validate binder classes, syntax spaces, syntax, and repetition alignment;
- represent lexical, recursive, and sequential contracts as immutable IR;
- apply fresh scopes to aligned binder and region captures;
- register identities in persistent environments with following visibility;
- thread multiple contracts without mutating captures or environments.

Evidence:

- the declarative `do` fixture compiles `$step.name` and `$rest` paths;
- invalid path, alignment, space, and syntax clauses emit SWR3002–SWR3005;
- runtime tests cover lexical field regions, recursive following declarations,
  sequential aligned rows, and ordered multi-contract execution;
- 253 tests pass across 49 files;
- formatting, lint, typecheck, package boundaries, and acceptance checks pass.

Next action: assign deterministic printed names in `HYG-006`.
