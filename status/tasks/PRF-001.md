# PRF-001 Evidence

Status: Done

Prerequisites: `RDR-007`, `TSH-007`

Specification: `docs/tasks/phase-07-tooling-release.md`, PRF-001

Delivered:

- define benchmark scenarios for read, expand, print, and incremental host work;
- capture runtime, platform, CPU, commit, and configuration metadata;
- support scenario filters, warmup counts, and measured sample counts;
- report stable statistical summaries as versioned JSON;
- compare results with checked-in baselines using explicit regression budgets.

Validation:

- three focused tests cover deterministic selection, warmup/sample isolation, raw CPU and heap samples, percentile summaries, and relative-plus-absolute regression budgets;
- six real reader and hygiene scenarios produced a five-sample Node 24.18.1 baseline;
- a non-mutating comparison run emitted structured regressions and returned failure without rewriting the baseline;
- the full repository gate passes 630 tests across 99 files plus all structural and acceptance checks.

Next action: extend the suite across compiler stages and optimize measured hot paths in `PRF-002`.
