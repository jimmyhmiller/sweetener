# REL-003 Evidence

Status: Done

Prerequisites: `TLS-005`, `PRF-002`

Specification: `docs/tasks/phase-07-tooling-release.md`, REL-003

Delivered:

- create two projects outside the pnpm workspace with no internal-path imports;
- validate clean setup and documented package consumption;
- exercise check, build, watch invalidation, and emitted runtime behavior;
- exercise diagnostics, hover, definitions, references, and safe rename;
- record cold/warm timings, defects, and release-blocking packaging gaps.

Validation:

- both samples install with `--ignore-workspace` and import only public package roots;
- the graph sample passes check, JavaScript/declaration build, macro watch invalidation, and downstream rebuild;
- the macro sample parses and compiles a declarative definition, expands with hygiene/resource accounting, emits origins/traces, type-checks, and runs to `[21, 21]`;
- mapped diagnostics, hover, definition, references, and safe rename pass;
- `samples:check` is part of the full gate, which passes 633 tests across 100 files.

Next action: replace link manifests with verified alpha tarballs in `REL-004`.
