# RDR-006 Evidence

Status: Done

Prerequisite: `RDR-004`

Specification: `docs/tasks/phase-01-reader.md`, RDR-006

Delivered:

- expose immutable source, reader options, read-file, and text-change types;
- implement `Reader.update` through a clean read;
- validate old range bounds, new length, unchanged prefix/suffix, and source
  identity;
- record strategy and zero-reuse baseline metadata;
- compare updated results with independent clean reads.

Evidence:

- clean and update results are immutable and retain source/version identity;
- update metadata records clean-read strategy, previous version, normalized
  range, and zero reused nodes;
- replacement, insertion, deletion, chained TSX edits, and independent clean
  reads produce structurally equal roots and diagnostics;
- invalid lengths, prefixes, suffixes, bounds, source IDs, file names, and
  source metadata are rejected;
- 137 tests pass across 31 files;
- typecheck and lint pass.

Benchmark: clean versus update cost and zero reuse are recorded in `RDR-007`.

Next action: run corpus, resource-limit, and performance baselines.
