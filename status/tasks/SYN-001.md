# SYN-001 Evidence

Status: Done

Prerequisite: `FND-002`

Specification: `docs/specifications/02-syntax-objects-hygiene.md`, section 1

Delivered:

- validated half-open spans and immutable trivia;
- stable token kinds, lexical modes, delimiters, categories, and precedence;
- immutable token, group, missing-close, and protected syntax nodes;
- constructor checks for malformed spans, token text, delimiters, recovery
  tokens, empty protected units, and mutable child nodes;
- structural hashing and equality that ignore instance IDs and source positions
  while retaining raw data, trivia, scopes, and origins;
- matcher literal equality that ignores trivia, positions, scopes, and origins.

Evidence:

- 74 tests pass across 25 files;
- a fixed hash vector protects cross-run stability;
- typecheck, lint, formatting, and package-boundary checks pass.

Benchmark: not applicable. This task defines data primitives; reader throughput
measurement begins in `RDR-007`.

Next action: use these nodes in `SYN-002` origins and `SYN-003` cursors.
