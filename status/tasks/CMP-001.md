# CMP-001 Evidence

Status: Done

Prerequisites: `EXP-004`, `HYG-003`

Specification: `docs/tasks/phase-05-composition.md`, CMP-001

Delivered:

- recursive macro bindings are visible while their definitions validate;
- local contexts enter a child expansion frame and expose an explicit unchanged
  exit environment;
- compiled macro lookup follows the nearest lexical expansion frame;
- guarded syntax-class recursion executes with class/cursor fingerprints;
- unguarded left recursion remains a stable definition-time diagnostic;
- local bindings cannot resolve after scope exit while parent bindings remain
  visible inside the region.

Validation: 485 tests pass across 69 files. Formatting, lint, TypeScript build,
package boundaries, acceptance contracts, recursive success, left-recursion,
and local scope-exit checks pass.

Next action: use these lexical context guarantees for generated definitions.
