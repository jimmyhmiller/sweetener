# MCL-001 Evidence

Status: Done

Prerequisites: `PAT-001`, `RDR-004`

Specification: `docs/tasks/phase-02-patterns.md`, MCL-001

Delivered:

- define immutable macro, class, rule, field, and parser-result records;
- parse export, recursion, categories, core-shadow clauses, fields, and rules;
- lower literal tokens, captures, groups, choices, repetition, and optional
  patterns to PAT-001 nodes;
- allocate deterministic capture, repetition, and cardinality IDs;
- recover at rule and definition boundaries with structured diagnostics.

Evidence:

- immutable definition, class, field, rule, clause, and parse-result records;
- deterministic definition, rule, syntax-class, capture, repetition, and
  cardinality IDs;
- syntax and operator headers, including recursion, categories, core shadows,
  word operators, and symbolic operators;
- PAT-001 lowering for literals, captures, groups, choices, separated
  repetition, and optional patterns;
- lossless retention of templates, binding directives, refinements, and
  operator properties for their owning phases;
- structured SWR2001-SWR2003 diagnostics with rule and definition recovery;
- all declarative acceptance files parse into one or more definitions;
- focused malformed-definition tests and 162 passing repository tests;
- formatting, lint, typecheck, package boundaries, acceptance ledger, and
  status validation pass.

Known reader recovery: the ADT template contains a macro repetition spelling
that the ordinary delimiter reader reports as SWR1003. MCL-001 still recovers
all three ADT definitions. Resolving template-aware delimiter interpretation
belongs to the template parser rather than this structural definition parser.

Next action: infer and validate capture shapes in `PAT-002`.
