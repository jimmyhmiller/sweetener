# CMP-005 Evidence

Status: Done

Prerequisites: `CMP-002`, `CMP-003`, `CMP-004`

Specification: `docs/tasks/phase-05-composition.md`, CMP-005

Current work:

- inventory every acceptance family and disabled evidence dimension;
- close families in dependency order with exact expansion, diagnostics,
  hygiene, TypeScript type, and runtime checks;
- add one TypeScript type-position macro absent from the JavaScript corpus;
- finish with the combined-language composition fixture.

Completed family checkpoints:

- `threading`: exact expansion, recursive parent/child trace, no introduced
  bindings, call-site hygiene, declarative malformed diagnostic, strict
  TypeScript inference, and runtime golden. Its accepted trailing comma is now
  represented by an explicit declarative rule.
- `implicit-return`: declarative `FunctionBody` alternatives, typed parameter
  binding, protected `#core` emission, trivia-safe `#trim`, exact malformed
  diagnostics, strict TypeScript output, and runtime result `7`.
- `operators`: executable precedence and associativity, exact expansion and
  trace evidence, malformed diagnostics, strict TypeScript/runtime checks, and
  a hygienic `globalThis.Object.is` equality helper.
- `rewritten-if`: complete block matching, definition-scoped runtime helper,
  exact malformed diagnostics, strict TypeScript output, and runtime result `3`.
- `currying`: modifier-aware lexical core dispatch, declarative trailing-comma
  parameter alternatives, protected overload emission, three binding
  contracts, adversarial printed-name hygiene, exact diagnostics, strict
  TypeScript overload inference, and both direct and partial runtime calls.

Completed core-rewrites checkpoint:

- `literal globalThis.NaN as NaN` lowers to a dedicated binding identity in
  matcher IR rather than textual token equality;
- compiled modules expose immutable binding-literal reference metadata;
- arity templates use finite declarative `#count(...)` operations.
- binding-aware `typeof` selects the global-NaN specialization while local
  shadows use protected core fallback;
- arity-checked functions reject extra runtime arguments and use capture-proof
  `globalThis.Number` and `globalThis.Error` definition references;
- exact expansion, bindings, source-order traces, diagnostics, TypeScript,
  hygiene, and runtime evidence are enabled.

Completed ADT/match checkpoint:

- constructor fields are modeled as structured syntax-class values, so each
  name/type pair shares one cardinality dimension without unsafe implicit
  zipping;
- nested constructor and field repetitions generate protected union and
  constructor item sequences with explicit type/value binding contracts;
- `#text` preserves internal syntax text while excluding peripheral call-site
  trivia, keeping generated discriminants stable across formatting;
- match arms use branch-local destructuring, allowing TypeScript discriminant
  narrowing and declarative pattern-binder scope without procedural code;
- exact structures, bindings, trace, malformed diagnostics, adversarial name
  assignment, strict semantic TypeScript, and runtime result `4` are enabled.

Completed protocols checkpoint:

- protocol declarations generate a correlated interface plus a self-contained,
  typed weak-map dispatch value using capture-proof global helpers;
- protocol subjects scope method parameter and result types with one binding
  identity, and method parameters retain their shared repetition shape;
- item-position infix operators are discovered at top-level left-operand
  boundaries and implementation parameters scope their method bodies;
- exact malformed diagnostics, strict semantic TypeScript, runtime dispatch,
  bindings, traces, and hygiene artifacts are enabled.

Completed CSP checkpoint:

- punctuation statement operators dispatch from the left operand boundary and
  select the longest declared compound token;
- declarative generator-context requirements reject send and receive forms
  outside generator expansion before template execution;
- receive declarations introduce their lexical value binding only into the
  following statement sequence;
- exact expansion, source-order traces, malformed diagnostics, adversarial
  printed-name hygiene, strict TypeScript, and generator runtime result `3` are
  enabled.

Completed multi-part methods checkpoint:

- recursive expansion recognizes `#syntax` output, compiles and registers its
  bindings into the live source-ordered environment, records generation traces,
  and erases compile-time definitions from runtime output;
- deterministic `#metavar(hint, $driver)` operations derive stable generated
  capture names from nested repetition indices without allocating runtime
  bindings or using procedural callbacks;
- generated rules preserve exact head and segment arity across newline-spanning
  mixfix calls, while declaration parameters retain hygienic body bindings;
- exact expansion, generated bindings, trace, missing-segment diagnostics,
  adversarial hygiene, strict TypeScript, and runtime result `true` are enabled.

Validation checkpoint: 557 tests pass across 80 files; formatting, lint,
TypeScript build, package boundaries, and acceptance contracts pass.

Next action: port `new-language` through module and function surface syntax,
class-element operators, self binding, exact diagnostics, hygiene, TypeScript,
and runtime evidence.

Completed combined new-language checkpoint:

- macro-aware item extent recognition preserves nested declarative invocations
  for recursive expansion without executing them during outer matching;
- modules recursively compose records, extension declarations bind method and
  receiver regions, and receiver dispatch lowers through the declared operator;
- exact nested diagnostics, module/record/method bindings, parented traces,
  adversarial hygiene, strict TypeScript, and runtime result `3` are enabled.

Final validation checkpoint: 562 tests pass across 81 files; formatting, lint,
TypeScript build, package boundaries, and every acceptance contract pass.

Next action: task complete; continue with `CMP-006` declarative-boundary audit.
