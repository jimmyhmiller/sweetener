# TPL-005 Evidence

Status: Done

Prerequisite: `HYG-006`

Specification: `docs/tasks/phase-03-hygiene-templates.md`, TPL-005

Delivered:

- a schema-valid conformance fixture with binding and trace artifacts;
- all eight required hygiene examples from specification section 13;
- `do`, match-branch, constructor, protocol-parameter, and generated-name cases;
- binding identity and scope assertions before printed-name assignment;
- explicit-capture trace and ambiguity diagnostic assertions;
- identity-allocation and unrelated-alpha-renaming invariance checks.

Evidence:

- template operations retain distinct fresh, captured, and definition scopes;
- three sequential clauses see zero, one, and two preceding bindings;
- match arms receive distinct scopes and export no following scope;
- constructor declarations export recursive following bindings;
- definition helpers resist call-site shadowing;
- value, type, runtime, and syntax-phase bindings resolve independently;
- 264 tests pass across 51 files;
- formatting, lint, typecheck, boundaries, and acceptance checks pass.

Next action: implement persistent expansion environments in `EXP-001`.
