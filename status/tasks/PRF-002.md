# PRF-002 Evidence

Status: Done

Prerequisites: `PRF-001`

Specification: `docs/tasks/phase-07-tooling-release.md`, PRF-002

Current work:

- add representative matcher, expansion, printing, origin-mapping, TypeScript handoff, and cache scenarios;
- capture CPU profiles for the dominant workloads;
- distinguish algorithmic costs from benchmark-harness overhead;
- implement representation or incremental-reuse changes only with measured evidence;
- record before/after reports and preserve semantic equivalence gates.

Next action: inventory public construction APIs and add dense declarative expansion plus origin-query benchmarks.

Progress:

- added matcher, generated-printer, incremental language-service, origin-mapping, and content-addressed-cache scenarios;
- measured matcher p50 52.58 ms, cache p50 28.70 ms, language-service edit p50 4.22 ms, and dense bidirectional mapping p50 306.76 ms;
- replaced quadratic reverse-origin scans with a per-source interval index;
- added conformance for overlapping, repeated, sparse, and zero-width intervals.

Validation:

- thirteen Node 24 scenarios cover reader, matcher, declarative expansion, lossless and generated printing, mapping, caches, hygiene, and incremental language service;
- cached scope-set append keys reduce the 5,000-add Node 24 p50 from 834.93 ms to 141.67 ms;
- interval indexing and identity-set construction reduce the identical dense mapping p50 from 306.76 ms to 23.09 ms on Node 26;
- raw CPU profiles preserve the attribution for mapping, expansion, and scope sets;
- all 631 tests and repository gates pass after the optimizations.

Next action: use the refreshed baseline for release evidence and future regression checks.
