# TSH-008 Evidence

Status: Done

Prerequisites: `TSH-007`

Specification: `docs/tasks/phase-06-typescript-host.md`, TSH-008

Current work:

- construct a multi-project macro/runtime dependency fixture;
- execute the test-architecture edit protocol through watch mode;
- perform a clean rebuild after every incremental result;
- compare expanded text, origin/source maps, diagnostics, declarations,
  JavaScript, traces, and runtime exports;
- assert dependency-minimal invalidation for each edit class.

Evidence:

- a reusable harness carries prior TypeScript programs across snapshots and
  compares every result with a clean build;
- comparisons include expanded text, high-resolution origin maps, serialized
  traces, diagnostics, JavaScript, declarations, both map kinds, and evaluated
  runtime exports;
- the matrix covers call-site text, macro-definition, unused macro export,
  runtime-only dependency, whitespace, configuration, and compiler-version
  changes;
- exact invalidation sets prove unused exports preserve dependents while every
  declared dependency reaches downstream projects;
- any byte or semantic mismatch fails with the responsible edit label.

Validation: 611 tests pass across 94 files with all substantive gates green.

Next action: task complete; Phase 6 exits and `TLS-001` begins.
