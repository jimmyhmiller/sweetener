# TLS-004 Evidence

Status: Done

Prerequisites: `TLS-003`

Specification: `docs/tasks/phase-07-tooling-release.md`, TLS-004

Delivered:

- map syntactic and semantic diagnostics to original macro sources;
- map quick-info spans and content back to source positions;
- map definitions across copied and introduced syntax;
- return expansion-view links for generated-only results;
- preserve TypeScript display parts, tags, and related information.

Validation:

- focused mapped-language-service tests cover diagnostics, quick info, definitions, and generated-only expansion links;
- canonical file keys tolerate TypeScript path normalization;
- complete generated spans project to complete source spans;
- 621 of 624 full-suite tests passed; the remaining three were successful compiler-heavy operations exceeding Vitest's default timeout under parallel load and now carry explicit 30-second budgets.

Next action: reuse the mapped facade for binding-aware references and safe rename in `TLS-005`.
