# ENF-003 Evidence

Status: Done

Prerequisite: `ENF-002`

Specification: `docs/tasks/phase-04-expansion.md`, ENF-003

Delivered:

- immutable pinned core prefix, postfix, and infix operator metadata;
- bounded Pratt parsing with left, right, and nonassociative behavior;
- conditional, assignment, arrow, optional comma, and update expressions;
- multi-token macro-operator resolution and expansion hooks;
- exponentiation, nullish/logical mixing, line-break, and parenthesis rules;
- production expression consumption in the structural syntax-class corpus.

Evidence:

- precedence trees distinguish multiplication/addition and left/right chains;
- conditional branches admit comma consequents and assignment alternates;
- malformed operands, missing colons, repeated nonassociative operators,
  unparenthesized unary exponentiation, and nullish mixing fail structurally;
- `|>` resolves as a one- or multi-token declarative operator head;
- eight representative outputs parse under pinned TypeScript 6.0.2;
- the Phase 2 `expr` placeholder is removed and structural fixtures pass;
- 358 tests pass across 56 files;
- formatting, lint, typecheck, boundaries, and acceptance checks pass.

Next action: implement macro invocation in `EXP-003`.
