# RDR-005 Evidence

Status: Done

Prerequisite: `RDR-004`

Specification: `docs/tasks/phase-01-reader.md`, RDR-005

Delivered:

- emit leading trivia, token spelling, trailing trivia, children, and real
  close tokens in exact tree order;
- omit synthesized missing tokens without omitting surrounding source;
- avoid recursive traversal for adversarial nesting;
- prove byte equality and read-print-read structural equality.

Evidence:

- byte equality and read-print-read structural equality cover ordinary
  TypeScript, templates, JSX, recovered delimiters, and unterminated input;
- every imported playground source round-trips byte-for-byte;
- 5,000 nested groups print without recursive traversal;
- 121 tests pass before the printer suite, and all focused reader tests pass
  with the new corpus cases;
- typecheck and lint pass.

Benchmark: traversal throughput and allocation are recorded in `RDR-007`.

Next action: enforce byte and structural equality in incremental updates.
