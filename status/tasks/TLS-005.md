# TLS-005 Evidence

Status: Done

Prerequisites: `TLS-004`, `HYG-004`

Specification: `docs/tasks/phase-07-tooling-release.md`, TLS-005

Delivered:

- resolve references with TypeScript symbol identity and map them to macro source;
- carry binding identities where origin metadata can prove them;
- classify copied, introduced, captured, and unmapped rename locations;
- return source edits only when every location preserves binding semantics;
- explain refusals and link generated-only locations to expansion views.

Validation:

- ordinary definitions, writes, and reads map to exact source spans;
- introduced syntax is refused with an expansion-view indication;
- copied captures without binding proof are refused, while a stable `BindingId` permits the edit;
- the full repository gate passes 627 tests across 98 files, plus formatting, lint, types, package boundaries, declarative boundaries, and acceptance contracts.

Next action: preserve the same conservative contract in future editor adapters.
