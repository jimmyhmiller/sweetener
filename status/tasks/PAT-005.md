# PAT-005 Evidence

Status: Done

Prerequisite: `PAT-004`

Specification: `docs/tasks/phase-02-patterns.md`, PAT-005

Delivered:

- record expected literals, groups, classes, lookahead predicates, and complete
  group boundaries at failed cursor identities;
- rank failures by farthest source position and specificity;
- merge equal-position expectations in deterministic order;
- memoize failed VM states with program counter, cursor identity, environment
  epoch, and repetition shape.

Evidence:

- immutable expectations cover literals, classes, groups, lookahead, and group
  completion;
- failures record source offset, cursor identity, specificity, definition
  origins, and sorted unique expectations;
- the VM retains only failures at the farthest offset and merges competing
  expectations there;
- terminal failure memo keys include PC, cursor identity, environment epoch,
  and repetition counts and capture lengths;
- converged control-flow paths reuse recorded failure states;
- environment epochs reject invalid values and isolate environment-sensitive
  matching sessions;
- successful matching and capture behavior remain unchanged;
- focused tests cover farther-position selection, expectation merging,
  specificity, memo hits, and epoch validation;
- 183 tests pass across 39 files;
- formatting, lint, typecheck, boundaries, and acceptance checks pass.

Next action: compile and execute user syntax classes in `PAT-006`.
