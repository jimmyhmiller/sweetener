# PAT-004 Evidence

Status: Done

Prerequisites: `PAT-003`, `SYN-003`

Specification: `docs/tasks/phase-02-patterns.md`, PAT-004

Delivered:

- define matcher inputs, syntax-class consumer boundaries, and result records;
- execute instructions on an explicit work stack;
- restore cursor, captures, and repetition state on failed choices;
- enter and require complete nested-group matches;
- enforce matcher-step and cancellation budgets.

Evidence:

- explicit worklist execution with forked cursors and capture maps;
- source-ordered split execution restores cursor, group, repetition, and
  capture state after failure;
- nested groups require the body to consume the complete child sequence before
  returning to the parent cursor;
- syntax-class consumers return captured syntax, fields, origins, and their
  advanced cursor;
- repeat frames implement greedy matching with rollback before a failed
  separator or iteration;
- zero-length and nested repetitions create immutable capture sequences at the
  inferred depth and cardinality group;
- matcher steps, deadlines, nesting depth, and cancellation stop execution;
- a threading acceptance definition passes reader, definition parsing, shape
  inference, compilation, and VM execution;
- focused tests cover literal matching, choice rollback, groups, repetition,
  empty and nested captures, limits, and cancellation;
- 180 tests pass across 39 files;
- formatting, lint, typecheck, boundaries, and acceptance checks pass.

Next action: add ranked failures and failed-state memoization in `PAT-005`.
