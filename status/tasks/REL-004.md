# REL-004 Evidence

Status: Blocked

Prerequisites: `REL-002`, `REL-003`

Specification: `docs/tasks/phase-07-tooling-release.md`, REL-004

Current work:

- select and apply an alpha package version consistently;
- generate publish manifests without private or `workspace:*` fields;
- pack every runtime package and install tarballs into clean consumers;
- publish compatibility matrix, benchmark report, known limitations, and versioning policy;
- assign explicit fixture, language, trace, manifest, and specification versions;
- produce a reproducible release manifest without contacting a registry.

Next action: implement non-mutating package staging and tarball-install verification.

Progress:

- staged thirteen synchronized `0.1.0-alpha.0` packages with rewritten exact internal dependencies;
- recorded byte length and SHA-256 for every tarball;
- installed every tarball together in a clean Node 24 consumer;
- reran both external sample workflows against installed tarballs;
- published local release notes, version policy, known limitations, compatibility, benchmarks, and explicit format versions;
- added a release integrity/documentation gate to `pnpm check`.

Remaining external actions:

- registry publication with the `alpha` dist-tag and provenance;
- annotated Git tag at the reviewed release commit.

Neither action is authorized. They have not been performed by the local
verifier.

Local completion evidence: all thirteen staged tarballs install together and
pass the external project, editor, CLI, build, declaration, watch, explanation,
and runtime workflows. Node 24 compatibility and the refreshed benchmark
regression budgets pass. No release publication or tag action may proceed
without separate, unambiguous maintainer authorization.
