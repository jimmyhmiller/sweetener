# RDR-001 Evidence

Status: Done

Prerequisite: `SYN-001`

Specifications: `docs/proposals/01-syntax-data-reader.md` and
`docs/tasks/phase-01-reader.md`, RDR-001

Delivered:

- define the lossless scanner-token contract;
- isolate supported TypeScript-version calls behind an adapter;
- preserve raw spelling, offsets, trivia, line breaks, and lexical mode;
- add token differential fixtures and explicit version rejection.

Evidence:

- TypeScript imports, scanner calls, syntax enums, and version classification
  are confined to `src/typescript-version/adapter.ts`;
- tests cover normalized kinds, exact spans and reconstruction, comments,
  Unicode, custom punctuation, EOF trivia, malformed strings, immutability,
  TS/JSX variants, and version rejection;
- 100 tests pass across 28 files;
- formatting, lint, typecheck, package boundaries, acceptance ledger, and
  status validation pass.

Benchmark: deferred to `RDR-007`, which measures the complete reader rather than
the scanner seam in isolation.

Next action: use the adapter's contextual rescans in `RDR-002` and `RDR-003`.
