# EXP-004 Evidence

Status: Done

Prerequisite: `EXP-003`

Specification: `docs/tasks/phase-04-expansion.md`, EXP-004

Delivered:

- allocation-independent structural input hashes and canonical fingerprints;
- active invocation-stack cycle detection keyed by binding, category, phase,
  admitted input, and environment epoch;
- balanced nesting and expansion-step accounting on all completion paths;
- shared tracker and cancellation contracts at the invocation boundary;
- cumulative output, matcher, template, depth, deadline, and cancellation limits;
- a complete-result cache that cannot publish a throwing or cancelled producer.

Evidence:

- fingerprints ignore syntax IDs, origins, spans, and scopes but distinguish
  structural token input;
- repeated active input fails while changed recursive input is permitted;
- changed recursion terminates at the global expansion-step limit;
- direct macro re-entry fails at the production invocation boundary;
- depth, cancellation, output-growth, and cycle exits restore nesting to zero;
- failed cache producers leave no visible entry and successful values hit;
- 370 tests pass across 58 files;
- formatting, lint, typecheck, package boundaries, and acceptance checks pass.

Next action: implement statement and item consumers in `ENF-004`.
