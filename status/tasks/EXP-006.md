# EXP-006 Evidence

Status: Done

Prerequisites: `HYG-005`, `ENF-005`, `EXP-004`

Specification: `docs/tasks/phase-04-expansion.md`, EXP-006

Current work:

- [x] inventory the imported and declarative `do` fixtures;
- [x] add one public compiler for syntax classes, matchers, templates, binding
      contracts, and hygienic macro bindings;
- [x] compile `Bind`, `BindAll`, and all recursive `doSteps` rules with no
      diagnostics;
- [x] connect matching, binding contracts, hygienic templates, and recursive
      generated-syntax expansion;
- [x] emit nested TypeScript for sequential bindings and final expressions;
- [x] retain parent-first recursive traces and structured malformed-clause
      diagnostics;
- [x] emit and validate destructuring and correlated parallel bindings;
- [x] assert introduced binding identities and parent-first traces;
- [x] verify runtime behavior, TypeScript inferred types, and hygienic printed
      collision names;
- [x] enforce expansion, bindings, trace, diagnostics, types, and runtime in the
      acceptance manifest.

Validation: 478 tests pass across 68 files. Formatting, lint, TypeScript build,
package boundaries, and acceptance-contract checks pass.

Next action: preserve this acceptance contract while implementing local and
generated syntax composition.
