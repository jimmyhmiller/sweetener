# CMP-002 Evidence

Status: Done

Prerequisites: `CMP-001`, `TPL-004`

Specification: `docs/tasks/phase-05-composition.md`, CMP-002

Delivered:

- explicit `#syntax { ... }` handoff from instantiated templates;
- ordinary parsing, compilation, validation, and definition-context re-entry;
- immutable provenance traces for marker, body, definitions, registrations, and
  environment epochs;
- source-ordered partial acceptance without leaking rejected bindings;
- hygienic captured names in generated definitions;
- malformed and unmarked output rejection without environment mutation.

Validation: 485 tests pass across 69 files. Formatting, lint, TypeScript build,
package boundaries, acceptance contracts, source ordering, provenance, captured
definition names, and rollback checks pass.

Next action: preserve the checked handoff while composing generated definitions
with custom operators and the remaining playground fixtures.
