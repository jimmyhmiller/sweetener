# CMP-004 Evidence

Status: Done

Prerequisites: `ENF-004`, `PAT-006`

Specification: `docs/tasks/phase-05-composition.md`, CMP-004

Delivered:

- executable newline-spanning mixfix segment matching;
- nested TypeScript calls inside every mixfix argument position;
- competing fixed-segment alternatives selected through ordinary rule order;
- farthest-boundary ranked failures across ambiguous alternatives;
- explicit proof that no separate mixfix registry or runtime is required.

Validation: three focused composition tests pass through the public macro
compiler and recursive expander. The complete repository gate is recorded in
the work log after status regeneration.

Next action: port the accepted multi-part-method family and the remaining
combined playground suite without adding a procedural macro boundary.
