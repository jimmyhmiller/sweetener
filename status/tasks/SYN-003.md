# SYN-003 Evidence

Status: Done

Prerequisite: `SYN-001`

Specification: `docs/specifications/02-syntax-objects-hygiene.md`, section 1

Delivered:

- immutable syntax-sequence snapshots and validated immutable ranges;
- peek, advance, consume, remaining-length, and end-position operations;
- constant-time marks and resets with cursor-instance ownership checks;
- independent forks that share stable traversal-location identities;
- nested group entry and exit with parent locations and depths;
- input and boundary validation for invalid offsets and group entry.

Evidence:

- ten cursor tests cover empty, bounded, forked, restored, nested, immutable,
  and independent traversal behavior;
- 91 tests pass across 27 files;
- typecheck and lint pass.

Benchmark: not applicable. Marks, resets, and forks store fixed-size cursor
state; end-to-end traversal throughput is measured in `RDR-007`.

Next action: use cursor identities and ranges in the reader and matcher.
