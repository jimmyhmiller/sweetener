# RDR-004 Evidence

Status: Done

Prerequisites: `RDR-002`, `RDR-003`, `SYN-002`

Specification: `docs/tasks/phase-01-reader.md`, RDR-004

Delivered:

- define an immutable file-root representation consistent with syntax nodes;
- convert scanner tokens to syntax tokens with source origins and empty scopes;
- group parentheses, brackets, braces, templates, and JSX containers using an
  explicit stack;
- diagnose unexpected and missing closers while retaining a partial tree.

Evidence:

- `RootSyntax` aligns the syntax union and architecture without fake source
  delimiters and retains EOF trivia;
- tests cover ordinary nesting, mismatched and unexpected closers, empty files,
  source and synthesized origins, templates, JSX elements and fragments, and
  exact reconstruction;
- a 2,000-level unterminated input recovers iteratively without stack overflow;
- 121 tests pass across 29 files;
- typecheck and lint pass.

Benchmark: deferred to `RDR-007`, including nesting depth and memory.

Next action: print these trees byte-for-byte in `RDR-005`.
