# TSH-009 Evidence

Status: Complete

Prerequisites: `TSH-001`, `TSH-002`, `TSH-007`, `TSH-008`

Specification: `docs/tasks/phase-06-typescript-host.md`, TSH-009

Current work:

- define the compile-time import syntax and connect it to manifest resolution;
- extract the category-complete expansion assembly from acceptance harnesses
  into a production compiler frontend;
- make project commands and inspection commands create that frontend by default;
- add an executable package entry point;
- prove build, declarations, runtime, mappings, explanations, watch
  invalidation, and clean/incremental equivalence in a clean external project.

Discovery evidence:

- `runConfiguredProjectCommand` currently requires an injected
  `ProjectExpansionProvider`;
- `runCli` currently requires injected expansion and inspection providers;
- `@sweetener/cli` currently declares no executable `bin`;
- existing external samples manually assemble low-level packages and therefore
  do not prove the default end-user workflow.

Progress:

- specified the language-version-1 source form as
  `import { exported as local } from "specifier" for syntax;`;
- implemented a structural parser which preserves imported/local names and
  source origins while leaving runtime imports unclaimed;
- added stable `SWR2004` diagnostics and focused valid, aliased, runtime, and
  malformed-import tests;
- documented the distinction between source import edges and manifest module
  contracts in the normative release surface.
- bound parsed imports to exact manifest exports with local aliases, source
  origins, duplicate-name checks, and stable module diagnostics;
- extracted the production expansion session used for expression, statement,
  item, type, binding, and class-element consumers;
- added typed variable declarations and function bodies to full-file
  enforestation so macros expand in normal initializer, annotation, and
  statement positions;
- implemented the default project provider, origin inspection data, default
  check/build/watch selection, and the `sweetener` executable;
- proved a no-provider project build with aliased expression, statement, and
  type imports, JavaScript/declaration emit, and runtime behavior;
- added an independent default-project workflow to ordinary sample and packed
  release verification.
- connected tsconfig path aliases and transitive project-local macro modules;
- integrated lexical symbolic operators into the production Pratt session and
  specified parenthesized operator imports;
- populated expansion stacks from production traces so default `explain`
  reports selected rules and captures;
- expanded the external executable proof to item macros, operators,
  `expand`/`explain`, and call-site plus macro-definition watch edits.
- added installed-package discovery through a versioned `sweetMacros` package
  pointer, manifest parsing, package-root/subpath resolution, compiler/language
  compatibility checks, and a no-provider package consumer test.
- validated package-root containment, declared dependencies, compiled exports,
  and manifests whose named exports live outside the package entry file;
- made watch dependency discovery include transitive project and installed
  package macro sources;
- enforced lexical macro and symbolic-operator visibility per defining module,
  including an operator used privately by an imported macro but unavailable to
  its caller;
- proved the default `.stsx` virtual-file path and reran the complete 644-test
  repository gate.
- exercised all twelve declarative playground families through the default
  production provider and executed their generated runtime contracts;
- added production coverage for generated definitions, recursive expansion,
  core opt-in, binding literals, generator context, JSX, and source ordering;
- synthesized invoked definition modules' ordinary runtime imports in consumer
  output, including relative-specifier rebasing and collision-safe aliases;
- ran all twelve malformed and hygiene contracts through the production provider
  and reran the rebuilt repository suite with 655 passing tests across 102 files.

Completion evidence: the clean external default project invokes the installed
`sweetener` executable without injected providers; expands expression,
statement, item, type, and operator forms; emits and executes JavaScript; emits
declarations and mappings; returns structured explanations; and rebuilds after
both call-site and macro-definition watch edits. Focused tests additionally
cover `.stsx`, package manifests, multi-source package exports, transitive watch
dependencies, diagnostic failures, and lexical module isolation.
The production playground matrix additionally proves every declarative family
through the default frontend and runtime, including hygienic definition-site
helper imports.
