# RDR-003 Evidence

Status: Done

Prerequisite: `RDR-001`

Specification: `docs/tasks/phase-01-reader.md`, RDR-003

Delivered:

- transition between JSX tag, text, and expression modes;
- preserve tag names, attributes, fragments, self-closing elements, and text;
- keep semantic tag pairing out of the scanner layer;
- retain exact raw source for mismatched and malformed input.

Evidence:

- tests cover tag and attribute names, text, expressions, fragments, nested and
  self-closing elements, templates and JSX nested in expressions, mismatched
  tags, and non-JSX comparisons;
- a scanner progress invariant rejects zero-width non-EOF transitions instead
  of hanging;
- exact reconstruction holds for every JSX case;
- 110 tests pass across 28 files;
- typecheck and lint pass.

Benchmark: deferred to `RDR-007` with the complete reader.

Next action: use lexical modes to construct delimiter trees in `RDR-004`.
