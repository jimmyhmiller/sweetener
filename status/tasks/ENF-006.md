# ENF-006 Evidence

Status: Done

Prerequisite: `ENF-004`

Specification: `docs/tasks/phase-04-expansion.md`, ENF-006

Delivered:

- protected `type` and `classElement` results with category macro hooks;
- bounded type extents for unions, intersections, conditionals, functions,
  queries, tuples, mapped types, indexed access, and nested generic closers;
- fields, methods, accessors, constructors, static blocks, decorators, index
  signatures, private names, explicit terminators, and class-field ASI;
- stable failure and cursor rollback for incomplete types and members;
- completed `OPEN-EXP-001` wrapper-versus-full-file validation experiment.

Evidence:

- 28 focused grammar cases pass with TypeScript differential validation;
- nested `>>` generic closers and caller-owned separators retain exact extents;
- adjacent invalid type atoms and unbalanced conditional/generic forms fail;
- category-specific macro heads intercept types and class elements;
- full-file parsing measured 1.27–2.03× faster than per-fragment wrappers;
- the provisional strategy and retained-heap results are documented in
  `docs/benchmarks/open-exp-001-fragment-validation.md`;
- 439 tests pass across 62 files;
- formatting, lint, typecheck, package boundaries, and acceptance checks pass.

Next action: implement statement-prefix/final-expression composition in
`ENF-007`.
