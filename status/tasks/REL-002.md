# REL-002 Evidence

Status: Done

Prerequisites: `CMP-006`, `REL-001`

Specification: `docs/tasks/phase-07-tooling-release.md`, REL-002

Delivered:

- audit normative grammar and declarative macro surface documentation;
- finalize hygiene, phase, operator, and generated-definition semantics;
- publish macro-module, trace, origin-map, security, and resource-limit contracts;
- document every public package and supported integration boundary;
- produce a migration guide from legacy and rewritten Sweet.js.

Validation:

- normative specification 06 covers all eight required release surfaces;
- ADR-0003 resolves fallback priority and ADR-0008 resolves fragment validation;
- duplicate ADR numbering was corrected and the measured scope optimization is ADR-0007;
- `check:release-spec` rejects unresolved specification markers, missing package/section contracts, and duplicate ADR numbers;
- the release-specification gate is part of `pnpm check`.

Next action: validate the published contracts from projects outside the compiler workspace.
