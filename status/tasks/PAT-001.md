# PAT-001 Evidence

Status: Done

Prerequisite: `SYN-003`

Specifications: `docs/specifications/03-patterns-templates.md`, section 1, and
`docs/tasks/phase-02-patterns.md`, PAT-001

Delivered:

- define immutable literal, capture, sequence, group, alternative, repetition,
  and class-call pattern nodes;
- represent capture paths, dimensions, and cardinality groups;
- define capture shapes and immutable records without matcher behavior;
- reject malformed paths, empty alternatives, invalid repetition bounds, and
  mutable children.

Evidence:

- immutable literal, capture, sequence, group, choice, repeat, optional,
  class-call, and fixed-lookahead nodes;
- spelling and binding literal keys remain distinct;
- capture paths retain source names and resolved IDs for field access;
- nested sequence shapes derive depth and preserve bounds and cardinality
  groups at each dimension;
- persistent shape and value records use deterministic capture-ID order;
- capture sequences reject mutable and dimensionally incompatible elements;
- tests cover all constructors plus invalid names, bounds, paths, choices,
  duplicates, and persistent updates;
- 158 tests pass across 36 files;
- full project gate passes.

Benchmark: not applicable. PAT-001 defines immutable data. PAT-004 and PAT-005
will measure matcher execution and memoization.

Next action: construct this IR from `.sts` definitions in `MCL-001`.
