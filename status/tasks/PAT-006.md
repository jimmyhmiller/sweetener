# PAT-006 Evidence

Status: Done

Prerequisite: `PAT-005`

Specification: `docs/tasks/phase-02-patterns.md`, PAT-006

Delivered:

- define compiled syntax-class records, public fields, and ordered rule sets;
- add token, token-tree, and identifier built-in consumers;
- execute user rules through the matcher VM and return named field records;
- validate missing fields, incompatible alternatives, unresolved classes, and
  unsafe recursive dependencies.

Evidence:

- parsed fields receive stable public capture IDs and resolved class IDs;
- parser results expose deterministic core and user class bindings;
- the macro-language adapter compiles all syntax classes in one forward-aware
  registry pass;
- token, token-tree, and identifier built-ins consume one syntax object with
  fixed semantics;
- external grammar consumers supply expression, statement, item, type,
  binding, class-element, and JSX-child matches;
- each class rule validates its required public field names, classes, and
  repetition shapes before matcher compilation;
- runtime rules run in source order and remap rule-local captures to public
  field records, including nested class fields;
- dependency analysis rejects unresolved classes and cycles that can recurse
  before consuming syntax, while guarded recursion remains legal;
- all acceptance syntax classes compile without diagnostics;
- the do-notation `BindClause` class matches source syntax and returns its
  `name` and `source` fields;
- 189 tests pass across 40 files;
- formatting, lint, typecheck, boundaries, and acceptance checks pass.

Next action: implement fixed declarative refinements in `PAT-007`.
