# RDR-007 Evidence

Status: Done

Prerequisites: `RDR-005`, `RDR-006`, `FND-005`

Specification: `docs/tasks/phase-01-reader.md`, RDR-007

Delivered:

- enforce token, nesting, deadline, and cancellation bounds;
- run imported playground and selected TypeScript/TSX corpora through
  read-print-read checks;
- record source bytes, token counts, diagnostics, samples, median, p95, range,
  throughput, heap, environment, commit, and command;
- compare clean reads with the zero-reuse update baseline.

Evidence:

- production TypeScript corpus: 37 files, zero diagnostics, byte-exact output,
  and deterministic rereads;
- imported playground corpus: all 13 files byte-exact, with one allowlisted
  missing parenthesis in `adt.sweet.js`;
- TSX corpus covers nested tags, attributes, text, expressions, and templates;
- 250 fixed seeds preserve bytes, structure, and diagnostic codes;
- cursor traversal reaches every nested syntax node in each playground tree;
- token, nesting, deadline, cancellation, 2,000-level recovery, and 5,000-level
  printing bounds pass;
- the seven-sample report records environment, commit, dirty state, source
  bytes, token counts, median, p95, range, peak and retained heap, and sampled
  allocations;
- 143 tests pass across 33 files before the final cursor assertion;
- full formatting, lint, typecheck, boundary, acceptance, and status gates pass.

Performance:

- macro-free TypeScript: 3.16 MiB/s and 625,402 tokens/s;
- playground syntax: 2.26 MiB/s and 584,513 tokens/s;
- TSX lexical modes: 1.44 MiB/s and 526,418 tokens/s;
- all workloads miss the provisional 25 MiB/s budget;
- `docs/benchmarks/reader-baseline.md` records method and profiling targets.

Next action: retain corpus gates and address throughput in `PRF-001` with a
pinned Node 24 runner.
