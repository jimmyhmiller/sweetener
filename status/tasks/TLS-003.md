# TLS-003 Evidence

Status: Done

Prerequisites: `TSH-003`, `TLS-001`

Specification: `docs/tasks/phase-07-tooling-release.md`, TLS-003

Current work:

- expose generated virtual snapshots through `LanguageServiceHost`;
- maintain monotonic script and project versions across source edits;
- resolve imports among generated and ordinary TypeScript files;
- retain printed files and origin-query indexes beside each snapshot;
- reuse TypeScript documents and macro caches for unchanged files.

Evidence:

- generated files are exposed through the official `LanguageServiceHost` and
  immutable `IScriptSnapshot`s;
- script/project versions advance monotonically for text, origin-map, or trace
  changes and remain stable for identical updates;
- virtual-to-virtual imports resolve through host file/directory APIs;
- semantic diagnostics refresh after edits while unchanged snapshots reuse the
  shared document registry;
- printed files remain retrievable for downstream origin queries.

Validation: 620 tests pass across 97 files with all substantive gates green.

Next action: task complete; begin `TLS-004` mapped language-service reads.
