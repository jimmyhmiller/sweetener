# PAT-002 Evidence

Status: Done

Prerequisites: `PAT-001`, `MCL-001`

Specification: `docs/tasks/phase-02-patterns.md`, PAT-002

Delivered:

- derive one capture shape for each rule pattern;
- preserve repetition depth and cardinality-group identity;
- compare capture sets and shapes across choices;
- diagnose duplicate captures, zero-width repetition, and incompatible syntax
  class fields without changing the parsed pattern tree.

Evidence:

- explicit-stack, bottom-up inference handles 5,000 nested nodes;
- capture leaves acquire syntax-class field shapes through a resolver;
- repeat and optional nodes wrap every body capture with the correct bounds and
  cardinality group;
- sequence merging reports duplicate capture IDs;
- alternatives require equal capture sets, classes, dimensions, bounds, and
  cardinality groups;
- progress analysis rejects looping bodies that can match without consuming
  syntax;
- class-rule validation checks required names, classes, and repetition shapes;
- parser IDs are stable by capture name within each rule, so alternatives share
  identities and duplicate occurrences are detectable;
- the declarative acceptance corpus parses and infers without pattern
  diagnostics;
- 168 tests pass across 37 files, including focused negative cases;
- formatting, lint, typecheck, boundaries, and acceptance checks pass.

Next action: compile validated patterns to matcher programs in `PAT-003`.
