# TSH-003 Evidence

Status: Done

Prerequisites: `TSH-001`, `TSH-002`

Specification: `docs/tasks/phase-06-typescript-host.md`, TSH-003

Current work:

- expose generated `.ts` files through a delegating TypeScript CompilerHost;
- create programs using ordinary TypeScript parsing, binding, and checking;
- preserve JavaScript, declaration, source-map, and build-info emission;
- keep virtual-file identity and generated metadata deterministic;
- test strict checking and declaration/JavaScript emit end to end.

Evidence:

- a canonical virtual-file overlay delegates all non-generated reads to the
  official TypeScript host;
- virtual directories participate in TypeScript module resolution, including
  imports between generated files;
- `createProgram` parses, binds, and strictly checks generated sources;
- captured host writes prove JavaScript, declarations, JavaScript maps,
  declaration maps, and `.tsbuildinfo` emission;
- generated-file metadata remains retrievable by canonical file identity;
- the host consumes the canonical TSH-002 printed artifact and can both capture
  emitted output for tooling and write it through to the delegate;
- ordinary TypeScript semantic diagnostics are preserved unchanged.

Validation: 590 tests pass across 87 files with formatting, lint, TypeScript,
package/declarative boundaries, and acceptance contracts green.

Next action: task complete; begin `TSH-004` diagnostic remapping.
