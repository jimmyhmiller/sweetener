# TLS-002 Evidence

Status: Done

Prerequisites: `TSH-002`, `TLS-001`

Specification: `docs/tasks/phase-07-tooling-release.md`, TLS-002

Current work:

- expose exact expanded TypeScript, origin-map, and trace views;
- parse file/line/column source queries;
- locate every corresponding generated region;
- render rule attempts, captures, binding resolution, hygiene operations,
  generated names, and nested invocation frames;
- produce stable human and machine-readable explanations.

Evidence:

- `expand` returns byte-exact generated TypeScript through an inspection
  provider;
- `explain` parses platform-safe one-based file/line/column positions and
  queries every generated occurrence;
- structured output includes nested invocation order, macro binding/category/
  phase, rule attempts and selection, captures, introduced/resolved bindings,
  hygiene operations, generated names, cache state, and core interception;
- unavailable sources/inspection providers fail explicitly;
- human command output and stable JSON explanations are both tested.

Validation: 618 tests pass across 96 files with all substantive gates green.

Next action: task complete; begin `TLS-003` language-service hosting.
