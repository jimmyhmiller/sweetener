# TSH-007 Evidence

Status: Done

Prerequisites: `TSH-004`, `TSH-006`

Specification: `docs/tasks/phase-06-typescript-host.md`, TSH-007

Current work:

- parse `tsconfig.json` plus the approved `sweet` project options;
- implement `check`, `build`, and persistent `watch` commands;
- traverse project references through TypeScript's official APIs;
- report remapped macro and TypeScript diagnostics consistently;
- expose cache hits, misses, dependency edges, and invalidations in debug mode.

Evidence:

- configuration loading validates every specified sweet option, rejects unknown
  keys, and discovers `.sts`/`.stsx` through TypeScript's config parser;
- check/build execute expanded virtual files through the official host and
  report both configuration and semantic diagnostics;
- build captures JavaScript and declaration artifacts while broken programs do
  not emit;
- project references execute dependency-first and cycles/missing references
  fail deterministically;
- watch observes configuration, sources, macro dependencies, and downstream
  reference dependents while retaining prior programs;
- command parsing and debug output expose stable operational state.

Validation: 610 tests pass across 93 files with all substantive gates green.

Next action: task complete; begin `TSH-008` incremental equivalence.
