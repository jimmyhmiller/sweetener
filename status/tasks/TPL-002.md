# TPL-002 Evidence

Status: Done

Prerequisite: `TPL-001`

Specification: `docs/tasks/phase-03-hygiene-templates.md`, TPL-002

Delivered:

- define template execution inputs and immutable output values;
- select repeated capture elements by aligned repetition indices;
- enforce runtime cardinality agreement;
- emit separators between successful iterations;
- evaluate optional presence and alternative tags declaratively;
- parse `#if(present ...)` and `#if(alternative ... "tag")` branches;
- charge template steps and honor cancellation, nesting, and deadlines;
- return immutable evaluated syntax and group pieces for hygienic instantiation.

Evidence:

- flat, separated, and nested repetition execution passes;
- runtime cardinality disagreement produces `TemplateCardinalityError`;
- presence and alternative branches select only fixed match metadata;
- template-step exhaustion and cancellation stop evaluation deterministically;
- 235 tests pass across 47 files;
- formatting, lint, and typecheck pass.

Next action: implement finite declarative operations in `TPL-003`.
