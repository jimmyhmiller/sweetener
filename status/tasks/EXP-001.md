# EXP-001 Evidence

Status: Done

Prerequisite: `HYG-002`

Specification: `docs/tasks/phase-04-expansion.md`, EXP-001

Delivered:

- immutable store-owned expansion-environment snapshots;
- syntax-binding indexes by spelling, phase, and all nine categories;
- persistent mixed-fixity operator tables and deterministic lookup;
- lexical child environments with fresh epochs and definition contexts;
- exact-key lexical shadowing and parent fixity inheritance;
- validation for ownership, runtime-space leakage, duplicates, and precedence.

Evidence:

- every extension receives a fresh environment ID and epoch;
- prior snapshots retain their original bindings and operators;
- item, statement, expression, type, binding, class, JSX, token, and token-tree
  categories map to distinct syntax spaces;
- local exact keys shadow parent keys without hiding unrelated fixities;
- 272 tests pass across 52 files;
- formatting, lint, typecheck, boundaries, and acceptance checks pass.

Next action: process source-ordered definition contexts in `EXP-002`.
