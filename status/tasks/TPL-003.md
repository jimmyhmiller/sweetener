# TPL-003 Evidence

Status: Done

Prerequisites: `TPL-002`, `HYG-004`

Specification: `docs/tasks/phase-03-hygiene-templates.md`, TPL-003

Delivered:

- define fixed operation IR without host callbacks;
- parse and validate `fresh`, `callsite`, `definition`, `capture`, and `text`;
- expose repetition indices and bounded template folds;
- record trace events for operations that alter ordinary hygiene behavior;
- preserve template-step, nesting, deadline, and cancellation accounting;
- compute stable syntax text without exposing compiler identities;
- evaluate bounded folds with fixed accumulator, element, and index locals.

Evidence:

- all five explicit hygiene operations parse and validate their argument class;
- invalid fresh hints, identifier operations, indices, and folds emit `SWR2017`;
- operation output and trace records are immutable and source ordered;
- fold bodies cannot call host code or introduce arbitrary locals;
- 241 tests pass across 47 files;
- formatting, lint, typecheck, boundaries, and acceptance checks pass.

Next action: materialize operation pieces in `TPL-004`.
