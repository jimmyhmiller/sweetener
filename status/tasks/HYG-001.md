# HYG-001 Evidence

Status: Done

Prerequisite: `FND-002`

Specification: `docs/tasks/phase-03-hygiene-templates.md`, HYG-001

Delivered:

- allocate fresh branded scope IDs;
- intern sorted immutable scope sets;
- implement add, remove, union, membership, and subset operations;
- serialize sets in stable debug order;
- test algebra laws and measure allocation and interning behavior.

Evidence:

- fresh scope IDs start outside the canonical empty-set ID and retain immutable
  kind and debug-label metadata;
- one store owns one canonical empty set and interns sorted unique scope arrays;
- add, remove, singleton, and union return existing IDs for equal sets;
- binary membership, linear subset, and linear merge avoid public enumeration;
- debug records and JSON serialization expose scopes in stable numeric order;
- unknown scope and set IDs fail at the store boundary;
- fixed-seed tests compare 2,000 generated mutations and subset checks against
  JavaScript `Set` values;
- tests assert union identity, idempotence, commutativity, and associativity;
- a 2,000-scope test exercises persistent allocation without mutable storage;
- the allocation benchmark records fresh scopes, singleton reuse, and a
  persistent 5,000-add chain;
- 203 tests pass across 42 files;
- formatting, lint, typecheck, boundaries, and acceptance checks pass.

Benchmark: `docs/benchmarks/scope-store-baseline.md` records 12,271,193 fresh
scopes/s, 3,220,342 singleton operations/s, and 8,338 persistent chain adds/s on
the Apple M2 Max under Node 26.5.0.

Next action: implement bindings and persistent environments in `HYG-002`.
