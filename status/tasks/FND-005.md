# FND-005 Evidence

Status: Done

Prerequisite: `FND-004`

Specification: `docs/proposals/00-expressiveness-contract.md`

Progress:

- imported 13 legacy source files with paths, hashes, and normalization notes;
- classified 12 acceptance families and assigned initial capability IDs;
- added an acceptance-intent schema and runtime loader;
- required each intent to reference declarative, expansion, type, runtime,
  hygiene, and malformed artifacts;
- required exact agreement between manifest and intent capability ledgers;
- completed proposed contracts for all twelve classified families;
- typechecked each primary and hygiene expansion with the pinned compiler API.

Validation:

- 53 tests pass across 24 files;
- corpus tests verify source hashes, provenance coverage, stable discovery,
  contract completeness, and proposed expansion types;
- the generated capability ledger covers every contract and open decision.

Next action: resolve the recorded phase-zero decisions in `FND-006`, then keep
the contracts executable as their compiler phases land.
