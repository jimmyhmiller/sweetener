# FND-001 Evidence

Status: Done

## Contract

Specification:

- `docs/specifications/01-compiler-architecture.md`, sections 1, 2, and 10

Task packet:

- `docs/tasks/phase-00-foundation.md#fnd-001-create-the-workspace`

## Work completed

- Accepted ADR-0001 with pnpm 11.18.0, Node 24 LTS, the TypeScript 6
  compatibility API, Vitest, ESLint, and Prettier.
- Created thirteen ESM package shells and strict TypeScript project references.
- Added root build, typecheck, lint, formatting, test, boundary, and status
  commands.
- Added package boundary and cycle detection with failure fixtures.
- Added one package smoke test per package and JSON unit-test reporting.
- Generated the lockfile with exact dependency versions.

## Validation required

```text
pnpm install --frozen-lockfile
pnpm build
pnpm typecheck
pnpm lint
pnpm test
pnpm check:boundaries
```

## Validation results

- build: pass;
- strict typecheck: pass;
- lint: pass;
- formatting: pass;
- unit tests: 16 pass across 14 files;
- package boundaries: pass;
- internal-import failure fixture: pass;
- package-cycle failure fixture: pass;
- status validation: pass.

The bootstrap ran on the machine's Node 26.5.0 and produced the expected engine
warning. ADR-0001 defines Node 24.18.0 as the supported environment.

## Decisions and deviations

- ADR-0001 accepted.
- TypeScript 7.0 has no programmatic API, so the workspace uses
  `@typescript/typescript6` 6.0.2 under the local `typescript` name.
- The compatibility package exposes `tsc6`; root scripts call that executable.
- No macro semantic behavior was implemented.

## Next action

Start `FND-002`: shared IDs, results, cancellation, and resource budgets.
