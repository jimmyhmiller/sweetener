# ENF-004 Evidence

Status: Done

Prerequisites: `ENF-003`, `EXP-003`

Specification: `docs/tasks/phase-04-expansion.md`, ENF-004

Delivered:

- immutable protected `stmt` and `item` consumer results;
- blocks, empty statements, expressions, control flow, declarations, and
  module-item extent recognition over delimiter trees;
- explicit semicolons, automatic insertion, and restricted line productions;
- category-specific macro resolution before built-in statement/item forms;
- sequential item-context processing under each current environment epoch;
- source-ordered runtime emission and compile-time definition registration.

Evidence:

- 25 focused statement/item cases cover valid and malformed forms;
- reconstructed valid extents parse under pinned TypeScript 6.0.2;
- ASI separates following-line statements without consuming their syntax;
- `return` line breaks terminate while `throw` line breaks fail;
- macro resolvers receive distinct `stmt` and `item` categories;
- item context updates the environment before consuming the next item;
- failed later items retain the committed prefix and caller cursor rollback;
- 397 tests pass across 60 files;
- formatting, lint, typecheck, package boundaries, and acceptance checks pass.

Next action: implement bindings and parameters in `ENF-005`.
