# SYN-002 Evidence

Status: Done

Prerequisite: `SYN-001`

Specification: `docs/specifications/02-syntax-objects-hygiene.md`, section 2

Delivered:

- source, copied, introduced, synthesized, and composed origin records;
- session-scoped deterministic allocation and structural interning;
- monotonic references that reject unknown and forward IDs, making cycles
  unrepresentable through the public API;
- iterative source collection over deep DAGs;
- invocation, definition, and leftmost primary-source policies;
- immutable origin and composed-part records.

Evidence:

- tests cover each variant, deduplication, invalid composition, forward links,
  source order, primary selection, independent sessions, and a 20,000-node
  chain;
- 81 tests pass across 26 files;
- typecheck and lint pass.

Benchmark: not applicable. `RDR-007` will record end-to-end origin overhead with
reader throughput and memory.

Next action: attach origin IDs to reader nodes after `SYN-003` supplies cursors.
