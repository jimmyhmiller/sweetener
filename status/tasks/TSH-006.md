# TSH-006 Evidence

Status: Done

Prerequisites: `TSH-001`, `TSH-003`

Specification: `docs/tasks/phase-06-typescript-host.md`, TSH-006

Current work:

- define reader, macro-module, and file-expansion content keys;
- include compiler/configuration and transitive invoked-macro hashes;
- track direct runtime and macro dependency invalidation sets;
- atomically publish only complete, uncancelled cache entries;
- expose deterministic hit/miss/invalidation evidence for watch tooling.

Evidence:

- cache keys cover reader, macro-module, and expansion stages plus source,
  compiler, configuration, direct macros, and transitive macro hashes;
- macro hash sets normalize order without erasing dependency identity;
- partial and cancelled results cannot enter the shared store;
- reverse dependency indexes invalidate every direct/transitive dependent and
  preserve independent records;
- immutable statistics expose hits, misses, commits, rejected publications,
  invalidations, and live entry counts.

Validation: 598 tests pass across 89 files with all substantive gates green.

Next action: task complete; begin `TSH-007` CLI commands.
