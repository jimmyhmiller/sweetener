# HYG-002 Evidence

Status: Done

Prerequisite: `HYG-001`

Specification: `docs/tasks/phase-03-hygiene-templates.md`, HYG-002

Delivered:

- define immutable binding records, syntax spaces, phases, and binding kinds;
- group TypeScript multi-space declarations under stable declaration IDs;
- implement persistent lexical environments and child frames;
- track source-order visibility regions and environment epochs;
- index candidates by spelling, phase, and syntax space for HYG-004 resolution.

Evidence:

- immutable binding records retain spelling, scope set, phase, syntax space,
  declaration origin, kind, group, and visibility;
- phases accept safe positive, zero, and negative integers;
- syntax spaces cover TypeScript value, type, namespace, and label bindings plus
  each macro category;
- declaration groups link multi-space bindings without merging their identities;
- whole-frame, from-position, and half-open range visibility filter temporal
  candidates;
- root, child, and declaration updates create persistent environment snapshots
  with unique IDs and epochs;
- child frames retain their parent snapshot when another branch updates the
  parent;
- candidate indexes key spelling, phase, and space, then walk lexical frames
  from nearest to farthest;
- environments reject snapshots owned by another store;
- tests cover persistence, temporal visibility, phases, spaces, groups, lexical
  ordering, validation, and cross-store rejection;
- 209 tests pass across 43 files;
- formatting, lint, typecheck, boundaries, and acceptance checks pass.

Next action: resolve invocation scope behavior in `HYG-003`.
