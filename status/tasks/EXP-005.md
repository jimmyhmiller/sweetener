# EXP-005 Evidence

Status: Done

Prerequisites: `EXP-003`, `ENF-004`

Specification: `docs/tasks/phase-04-expansion.md`, EXP-005

Delivered:

- pinned core-form identities by syntax category and phase-aware lookup;
- explicit local `shadows core` authorization and two-sided import opt-in;
- lexical dispatch with built-in fallback and structured ambiguity diagnostics;
- guarded invocation validation for intercepted bindings;
- core-interception provenance retained in success and failure traces;
- focused coverage for local scope, imports, ambiguity, malformed configuration,
  category separation, fallback, and trace validation.

Validation: 468 tests pass across 65 files. Formatting, lint, TypeScript build,
package boundaries, and acceptance-contract checks pass.

Next action: reuse the dispatch and trace contract in the `do` vertical slice
and later operator integration.
